{
  "name": "ennabl-design-data",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "validate:schemas": "ts-node mock/schemaValidator.ts",
    "mock:validate": "ts-node mock/schemaValidator.ts",
    "seed:tenant": "npm run -s mock:validate && ts-node mock/seedTenant.ts",
    "reset:tenant": "ts-node mock/resetTenant.ts",
    "reseed:tenant": "npm run -s mock:validate && ts-node mock/reseedTenant.ts"
  },
  "dependencies": {
    "@faker-js/faker": "^9.0.0",
    "@supabase/supabase-js": "^2.45.4",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}


